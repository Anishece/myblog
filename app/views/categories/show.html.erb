<h1><%= @category.name %></h1>
<%= if @posts.each do |post| %>
   <h3><%= link_to post.title,post %></h3>
   <p><%= post.body %></p>
<% end.empty? %>
   <p>No,posts available</p>
<% end %>
<%= link_to "Back to catagories",categories_path %>